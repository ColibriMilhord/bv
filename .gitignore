# Journaux applicatifs — peuvent contenir des données personnelles (e-mails clients)
*.log

# Cache applicatif (agenda Datatourisme)
cache/*
!cache/.gitkeep

# Fichiers de debug temporaires
dt_debug.json

# Secrets applicatifs : ne jamais versionner
config/secrets.php
